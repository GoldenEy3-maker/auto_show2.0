export type InteractionElementsStyleType = 'normal' | 'danger' | 'gray'

export type CartData = {
  id: number
  img: string
  title: string
  text: string
  quantity: number
  maxCount: number
  price: number
}